# GitLab CI/CD Configuration for CanvApps
image: node:20

pages:
  stage: deploy
  script:
    - npm install
    - npm run build:lib
    - npm run build:preview-code
    - rm -rf public
    - mv preview-code/bundle public
  artifacts:
    paths:
      - public
  only:
    - main
